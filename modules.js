// Core modules

const fs = require("fs");
const os = require("os");

// fs.writeFileSync("dummy.txt" , "Learning Node Modues");
// console.log(os.platform());
// console.log(os.hostname());
// console.log(os.cpus());

// Global objects
console.log(process.cwd());
console.log(process.pid); 